const producto = 'Monitor hp 20" ';
console.log(producto.toUpperCase()); // Pasar un string a Mayuscula
console.log(producto.toLowerCase());// Pasa todo a minisculas

const precio = 300;
console.log(precio);
console.log(precio.toString());
