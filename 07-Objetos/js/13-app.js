const producto = {
    nombre: 'Monitor de 20 Pulgadas',
    precio: 300,
    disponible: true,
    }

    console.log(producto);

console.log(Object.keys(producto)); // Conocer las llaves de un objeto

console.log(Object.values(producto)); // Conocer los valores de un objeto

console.log(Object.entries(producto)); // Retorna todo


